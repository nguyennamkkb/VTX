import 'package:flutter/material.dart';

class XacNhanDat extends StatelessWidget {
  const XacNhanDat({Key? key}) : super(key: key);

  @override
  Widget build(BuildContext context) {
    return Scaffold(
      appBar: AppBar(),
      body: Container(
        child: Column(
          children: [
            Text("ádasd")
          ],
        ),
      ),
    );
  }
}
